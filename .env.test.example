# ===========================================
# E2E Test Environment Variables
# ===========================================
# Kopieer dit bestand naar .env.test en vul de waarden in
# BELANGRIJK: .env.test staat in .gitignore en wordt NOOIT gecommit

# -----------------------------------------
# Supabase Configuration
# -----------------------------------------
VITE_SUPABASE_URL=https://wiuzjpoizxeycrshsuqn.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# -----------------------------------------
# Test User Credentials
# -----------------------------------------
# Use an existing admin user for testing
# DO NOT create new users - use existing account
TEST_USER_EMAIL=your-admin@example.com
TEST_USER_PASSWORD=your-test-password-here

# -----------------------------------------
# NOTES
# -----------------------------------------
# 1. TEST_USER should be an existing admin account in the database
# 2. SERVICE_ROLE_KEY is only for E2E test setup/cleanup - never use in frontend
# 3. If these variables are not set, auth-dependent tests will be skipped gracefully
# 4. Keep this file secure - it contains sensitive credentials
